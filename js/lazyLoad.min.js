"use strict";const sections=document.querySelectorAll(".lazy");function handleIntersection(e,s){e.forEach((e=>{e.isIntersecting&&(e.target.classList.add("visible"),s.unobserve(e.target))}))}const observer=new IntersectionObserver(handleIntersection,{threshold:.15});sections.forEach((e=>{observer.observe(e)}));